<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
                android:layout_width="match_parent"
                android:id="@+id/fra_forecast_main_container"
                android:layout_height="match_parent"
                android:background="@drawable/bg_weather"
                android:orientation="vertical"
                android:paddingTop="30dp">

    <ImageView
        android:id="@+id/fra_forecast_arrow_back"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_margin="10dp"
        android:layout_marginRight="10dp"
        android:background="@drawable/ic_white_back"/>

    <TextView
        android:id="@+id/fra_forecast_title"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_toRightOf="@id/fra_forecast_arrow_back"
        android:text="Title"
        android:textColor="@android:color/white"
        android:textSize="20sp"/>


    <TextView
        android:id="@+id/fra_forecast_subtitle"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignLeft="@id/fra_forecast_title"
        android:layout_below="@id/fra_forecast_title"
        android:text="@string/weather_forecast"
        android:textColor="@android:color/white"
        android:textSize="14sp"/>

    <android.support.v7.widget.RecyclerView
        android:id="@+id/fra_forecast_recyclerview"
        xmlns:android="http://schemas.android.com/apk/res/android"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_below="@id/fra_forecast_subtitle"
        android:layout_marginTop="10dp"
        android:drawSelectorOnTop="true"
        android:paddingTop="2dp"/>

</RelativeLayout>